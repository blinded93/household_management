<nav class="navbar navbar-light ">
  <button class="navbar-toggler navbar-toggler-right" type="button" data-toggle="collapse" data-target="#navbarNavDropdown" aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>
  <h1 class="navbar-brand">HouseholdManagement - <small class='text-muted'><%= current_household.try(:address) %></small></h1>
  <div class="collapse navbar-collapse" id="navbarNavDropdown">
    <ul class="navbar-nav">
      <li>
        <%= link_to "Home", household_member_path(current_household, current_member), class:'nav-link' %>
      </li>
      <li>
        <%= link_to "Members", household_members_path(current_household), class:'nav-link' %>
      </li>
      <% if current_member.head_of_household %>
        <li>
          <%= link_to "Household Control", household_control_path(current_household), class:'nav-link' %>
        </li>
        <li>
          <%= link_to "Rooms", household_rooms_path(current_household), class:'nav-link' %>
        </li>
        <li>
          <%= link_to "Bills", household_bills_path(current_household), class:'nav-link' %>
        </li>
      <% end %>
      <li>
        <%= link_to "Logout", logout_path, class:'nav-link', method: 'delete', data:{confirm: "Are you sure you want to log out?"} %>
      </li>
    </ul>
  </div>
</nav>
