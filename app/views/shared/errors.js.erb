<% obj.completed_fields.each do |field| %>
  <% if field != :item_id || :password %>
    $("#<%= obj.form_input_id(field) %>").removeClass("border border-danger");
  <% end %>
<% end %>
<% obj.errors.messages.each do |field, error| %>
  $("#<%= obj.form_input_id(field) %>").addClass("border border-danger");
<% end %>
$("#<%= obj.error_div_id %>").html("<%= j render 'shared/errors', obj:obj %>");
