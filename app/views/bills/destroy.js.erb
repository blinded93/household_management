$(".<%= bill.row_id %>").remove();
<% Bill.scopes.keys.each do |scope| %>
  $("#<%= badge_id(scope, bill) %>")
      .html("<%= bills.send(scope).count %>");
<% end %>
$("#remaining-bills-amount")
    .html("Remaining Monthly Expenses: <%= remaining_expenses %>");
