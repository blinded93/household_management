$(".<%= bill.row_id %>").remove();
<% Bill.scopes.keys.each do |scope| %>
  $("#<%= "#{scope.to_s}_bills_badge" %>").html("<%= bills.send(scope).count %>");
<% end %>
$("#remaining_bills_amount").html("Remaining Monthly Expenses: <%= remaining_expenses %>");
