<span class="card-title">
  <u class='lg-font'>
    Inventory
  </u>
  <span class='pb-2'>
      <%= add_btn(:room_item, current_household) %>
      <%= refresh_btn(current_household, :room_items) %>
  </span>
</span>

<div id="accordion-inventory" data-children=".room-item">
  <%= render partial:'room_items/room_items_div',
        collection:current_household.rooms.unowned,
        as: :room %>
</div>
