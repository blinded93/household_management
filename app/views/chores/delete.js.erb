<% chore_ids.each do |id| %>
  $(".chore_<%= id %>").remove();
<% end %>
<% obj.class.scopes.keys.each do |scope| %>
  $("#<%= "#{scope.to_s}_chores_badge" %>").html("<%= objs.send(scope).count %>");
<% end %>
