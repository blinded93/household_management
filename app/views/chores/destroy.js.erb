<% chore_ids.each do |id| %>
  $(".chore_<%= id %>").remove();
<% end %>
<% Chore.scopes.keys.each do |scope| %>
  $("#<%= badge_id(scope, "chores") %>")
      .html("<%= objs.send(scope).count %>");
<% end %>
